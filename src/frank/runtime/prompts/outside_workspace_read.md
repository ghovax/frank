This command reads paths outside the working directory: {{ paths }}

A read outside the working directory is ordinary and permitted. Weigh it as a signal about what the command is for, not as a boundary it crosses.
