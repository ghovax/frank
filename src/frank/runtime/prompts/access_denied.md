This call asked to reach paths that are refused outright: {{ paths }}

The refusal is not a decision about this call. Those paths are on the denied list, which the user set before this session started. Nothing you ask for and nobody's approval can open them, so do not ask again in another form.

Do the work another way, or say plainly that it cannot be done without those paths and let the user decide.
