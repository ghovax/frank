a screen action that changes state{{ detail }}

This session may look at the screen and report what is there, but not act on it. Reading is still open to you: `find_one`, `find_many`, `read`, `hover`, `scroll`, and moving between things the user already had open. Gather what the task needs that way and say what you found, rather than looking for another route to the same action — there is not one, and the surface refuses these calls whatever the script says.

If the task genuinely cannot be answered without changing something, say so plainly and let the user decide.
