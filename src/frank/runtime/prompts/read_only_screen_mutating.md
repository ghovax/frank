a screen action that changes state{{ detail }}

This session may look at the screen and report what is there. It may not act on what it sees. Reading stays open to you: `find_one`, `find_many`, `read`, `hover`, `scroll`, and movement between things the user already opened. Gather what the task needs that way, and say what you found.

Do not look for another route to the same action. There is none. The surface refuses these calls whatever the script says.

If the task truly cannot be answered without a change, say so plainly and let the user decide.
