{{ app }} does not publish its windows to macOS accessibility, so `{{ target }}` names the application rather than any one of its windows — there is nothing there to read or act on. {{ detail }}

This is the application's own choice and nothing here can change it while it runs. Take a screenshot to see it and ask the user to do the step, or reach it another way if it has an API or a command line. Don't act blind.
