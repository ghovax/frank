`{{ primitive }}` is not something this session may do. It is refused here, at the surface, rather than by reading your script — so there is no way to phrase it that would go through, and nothing before this line was affected.

This session is read-only: it can look at the screen and report what is there, but not act on it. What it can call is {{ available }}.

If the task genuinely needs to change something, say so in your answer and let the user decide; do not work around it.
