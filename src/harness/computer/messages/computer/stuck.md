Your last few actions changed nothing on screen, so repeating them will not help. Stop and look again with observe, then get in a different way: act on a different element, switch to another window or app (a keyboard shortcut, or the bash tool with `open`), reach the command through the menu bar or its keyboard shortcut, or scroll the target into view. If the app genuinely exposes nothing to read, take a screenshot to see it and ask the user to do the step.
