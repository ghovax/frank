"""The harness FastAPI server package: the app, its shared runtime state, and (as it
 is split out) the route modules. Kept importable as ``harness.server.app`` so route
 handlers and routers share one module identity and one set of runtime singletons."""
