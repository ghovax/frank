The `edit_file` tool could not find the supplied `find` text in `{{ resolved_path }}`.

Copy it **character-for-character** from the most recent `read_file` output — including every space, quote, and indentation. Whitespace differences (trailing spaces, tabs vs spaces, indentation levels) are the most common cause. Do not retype the text from memory.
