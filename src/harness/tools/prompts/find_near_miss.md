The `edit_file` tool could not find the exact `find` text in `{{ resolved_path }}`, but found {{ count }} similar match(es):

{{ candidates }}

Copy the exact text from the `read_file` output — pay close attention to indentation, spacing, line endings, and punctuation. Use the candidate locations to locate the correct block. Do not edit the file by typing text manually; always copy from the file.
