**List the tools** exposed by configured MCP servers.

Pass a `server` name to inspect one server, or leave it empty to list every enabled server. Use this to **discover** what a configured MCP server actually exposes before calling `call_mcp_tool`.

A short **justification** is welcome when the purpose is not obvious from the arguments — one flowing open-ended clause, never a `label: detail` heading.
