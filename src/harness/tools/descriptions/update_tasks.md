**Update the status** of one or more tasks at once.

For each update, move the task to `in_progress` when you start it, `completed` (with a one-line `result`) when it is **actually done**, and `blocked`/`cancelled` when reality diverges from the plan.

- Do **not** leave tasks sitting in their initial state while you finish the work around them.
- **Never** end the turn with steps still unresolved that you in fact completed — reconcile first.
- Update on **genuine state changes**, not as busy-work.
