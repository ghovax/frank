**Read a resource** exposed by a configured MCP server, by its URI.

Discover the available URIs with **list_mcp_resources** first, then read one here.

A short **justification** is welcome when the purpose is not obvious — one continuous open-ended phrase, not a `label: detail` heading.
