# Consolidate memory

The structured memory below has grown large. Record a smaller, cleaner version of it that loses nothing that still matters:

- Merge related entries and collapse superseded state into its final form.
- Drop entries no longer relevant to ongoing or future work.
- Keep durable facts, decisions, concrete identifiers (paths, ids, names, commands, numbers), the user's goals and preferences, and every still-open thread.
- Preserve the original ordering where chronology matters.

## Current memory

```json
{{ observations }}
```
