The tool call `{{ name }}` was malformed and was not executed — its arguments could not be parsed ({{ error }}), so it had no effect. Do not repeat the same malformed payload. Re-issue the call with valid JSON arguments that parse cleanly.
