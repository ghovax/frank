An agent you contacted became unavailable before it could answer, so the harness is returning control to you instead of leaving you waiting indefinitely.

Your question to **{{ sender_agent_name }}** (`{{ sender_task_identifier }}`) could not be answered because that agent is no longer active.

**Message identifier:** `{{ message_identifier }}`

**Next step:** Continue without the response, or delegate replacement work if it is still necessary.
