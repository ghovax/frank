The finding itself in one line, written so that it can be judged on its own.

A later pass sees only this line when deciding whether the entry still holds, so it has to stand without its detail behind it.
