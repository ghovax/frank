The finding's durable meaning in one synthesized line, written so that it can be judged on its own. Never copy answer prose, raw output or a collection of items.

A later pass sees only this line when deciding whether the entry still holds, so it has to stand without its detail behind it.
