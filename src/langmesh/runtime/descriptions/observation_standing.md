How well this is established, which the next reader cannot recover once the turns are gone.

- `verified` — you saw the proof yourself.
- `reported` — something claimed it and you did not check.
- `inferred` — you concluded it.

Something that ran without an error is `reported`, never `verified`. A number you did not watch being measured is `reported`.
