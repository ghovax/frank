The instruction itself in one line, written so that it can be judged on its own.

Their meaning, not their words: never paste the message back. A later pass sees only this line when deciding whether the instruction still binds, so it has to stand without its detail behind it. What it rules in and out, and why, goes in `detail`.
