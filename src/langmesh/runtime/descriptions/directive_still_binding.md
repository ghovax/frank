False only where a later message plainly lifted this instruction.

An instruction that has been carried out still binds: it is why the work looks the way it does, and the next model must not undo it.
