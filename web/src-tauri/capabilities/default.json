{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main Daisy window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
